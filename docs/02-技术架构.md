# GoldPilot 技术架构文档

## 一、系统架构

### 1.1 整体架构图
```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (React)                        │
│  - Vite + React 18 + TypeScript                            │
│  - Lightweight Charts (K线图)                               │
│  - Socket.IO Client (实时通信)                              │
│  - TailwindCSS (样式)                                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ WebSocket / REST API
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      后端层 (Node.js)                        │
│  - Express.js + Socket.IO                                   │
│  - MetaAPI (MT4/MT5对接)                                    │
│  - Alpha Vantage API (行情数据)                             │
│  - MongoDB (数据存储)                                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、前端架构

### 2.1 技术选型

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.3 | UI框架 |
| TypeScript | 5.4 | 类型安全 |
| Vite | 5.2 | 构建工具 |
| TailwindCSS | 3.4 | 样式框架 |
| Lightweight Charts | 4.1 | K线图库 |
| Socket.IO Client | 4.7 | WebSocket客户端 |
| Axios | 1.6 | HTTP客户端 |
| Zustand | 4.5 | 状态管理（可选） |

### 2.2 前端目录结构
```
goldpilot-frontend/
├── src/
│   ├── components/          # 组件
│   │   ├── Header/         # 顶部导航
│   │   │   ├── Header.tsx
│   │   │   └── index.ts
│   │   ├── PriceCard/      # 价格卡片
│   │   ├── Chart/          # K线图
│   │   │   ├── Chart.tsx
│   │   │   ├── Chart.types.ts
│   │   │   └── index.ts
│   │   ├── SignalPanel/    # 信号面板
│   │   ├── AccountCard/    # 账户卡片
│   │   ├── EventList/      # 事件列表
│   │   └── DecisionCard/   # 决策卡片
│   ├── hooks/              # 自定义Hooks
│   │   ├── useWebSocket.ts
│   │   ├── useSignals.ts
│   │   └── useAccount.ts
│   ├── services/           # API服务
│   │   ├── api.ts          # REST API
│   │   ├── websocket.ts    # WebSocket
│   │   └── types.ts        # API类型定义
│   ├── utils/              # 工具函数
│   │   ├── indicator.ts    # 指标计算
│   │   ├── format.ts       # 格式化
│   │   └── constants.ts    # 常量
│   ├── types/              # TypeScript类型
│   │   ├── signal.ts
│   │   ├── market.ts
│   │   └── account.ts
│   ├── context/            # Context
│   │   └── AppContext.tsx
│   ├── App.tsx             # 根组件
│   └── main.tsx            # 入口文件
├── public/                 # 静态资源
│   └── favicon.ico
├── index.html
├── package.json
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.json
└── .env.example
```

### 2.3 核心组件说明

#### Chart组件
```typescript
// src/components/Chart/Chart.tsx
interface ChartProps {
  candles: Candle[];
  signals: Signal[];
  period: string;
  onPeriodChange: (period: string) => void;
}

interface Candle {
  time: Date;
  open: number;
  high: number;
  low: number;
  close: number;
  volume: number;
}

interface Signal {
  time: Date;
  direction: 'long' | 'short';
  price: number;
  type: 'entry' | 'exit';
}
```

---

## 三、后端架构

### 3.1 技术选型

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | 20 LTS | 运行环境 |
| Express.js | 4.19 | Web框架 |
| Socket.IO | 4.7 | WebSocket服务 |
| MongoDB | 7.0 | 数据库 |
| Mongoose | 8.3 | ODM |
| TypeScript | 5.4 | 类型安全 |
| PM2 | 5.3 | 进程管理 |

### 3.2 后端目录结构
```
goldpilot-backend/
├── src/
│   ├── config/             # 配置
│   │   ├── database.ts     # MongoDB配置
│   │   ├── mt4.ts          # MetaAPI配置
│   │   └── index.ts        # 统一导出
│   ├── controllers/        # 控制器
│   │   ├── price.ts        # 价格相关
│   │   ├── signal.ts       # 信号相关
│   │   ├── account.ts      # 账户相关
│   │   └── index.ts
│   ├── models/             # 数据模型
│   │   ├── Signal.ts       # 信号模型
│   │   ├── Account.ts      # 账户模型
│   │   ├── DailyStats.ts   # 每日统计
│   │   └── index.ts
│   ├── routes/             # 路由
│   │   ├── api.ts          # REST API路由
│   │   └── index.ts
│   ├── services/           # 业务逻辑
│   │   ├── indicator.ts    # 指标计算
│   │   ├── metaapi.ts      # MT4服务
│   │   ├── market.ts       # 行情服务
│   │   └── index.ts
│   ├── middleware/         # 中间件
│   │   ├── error.ts        # 错误处理
│   │   ├── logger.ts       # 日志
│   │   └── index.ts
│   ├── utils/              # 工具函数
│   │   ├── logger.ts       # 日志工具
│   │   └── index.ts
│   ├── types/              # TypeScript类型
│   │   └── index.ts
│   ├── websocket/          # WebSocket
│   │   ├── handler.ts      # WS处理
│   │   └── index.ts
│   └── app.ts              # 入口文件
├── logs/                   # 日志文件
├── .env                    # 环境变量
├── .env.example            # 环境变量示例
├── ecosystem.config.js     # PM2配置
├── package.json
└── tsconfig.json
```

### 3.3 API接口设计

#### REST API
```
GET  /api/price              # 获取实时价格
GET  /api/candles            # 获取K线数据
GET  /api/signals            # 获取信号列表
GET  /api/stats/today        # 获取今日统计
GET  /api/account            # 获取账户信息
```

#### WebSocket事件
```typescript
// 客户端 → 服务端
emit: 'subscribe:price'
emit: 'subscribe:signals'
emit: 'subscribe:account'

// 服务端 → 客户端
on: 'price'          // 价格更新
on: 'signal'         // 新信号
on: 'account'        // 账户更新
```

---

## 四、数据库设计

### 4.1 数据模型

#### Signal（信号记录）
```typescript
{
  _id: ObjectId;
  timestamp: Date;           // 信号时间
  direction: 'long' | 'short'; // 方向
  entry_price: number;       // 入场价
  exit_price: number;        // 出场价
  profit: number;            // 盈亏金额
  status: 'pending' | 'profit' | 'loss'; // 状态
  period: string;            // 周期
  atr: number;               // ATR值
  created_at: Date;
  updated_at: Date;
}
```

#### Account（账户信息）
```typescript
{
  _id: ObjectId;
  account_id: string;
  server: string;
  balance: number;
  equity: number;
  margin: number;
  free_margin: number;
  positions: Position[];
  daily_pnl: number;
  updated_at: Date;
}
```

#### DailyStats（每日统计）
```typescript
{
  _id: ObjectId;
  date: Date;
  signal_count: number;
  win_count: number;
  loss_count: number;
  win_rate: number;
  total_profit: number;
  total_loss: number;
  net_profit: number;
}
```

---

## 五、核心算法

### 5.1 EMA计算
```typescript
/**
 * 计算EMA指数移动平均线
 * @param data - 价格数组
 * @param period - 周期
 * @returns EMA值
 */
function calculateEMA(data: number[], period: number): number {
  if (data.length < period) return 0;

  const k = 2 / (period + 1);
  let ema = data[0];

  for (let i = 1; i < data.length; i++) {
    ema = data[i] * k + ema * (1 - k);
  }

  return ema;
}
```

### 5.2 ATR计算
```typescript
/**
 * 计算ATR平均真实波幅
 * @param candles - K线数据
 * @param period - 周期（默认14）
 * @returns ATR值
 */
function calculateATR(candles: Candle[], period: number = 14): number {
  if (candles.length < period + 1) return 0;

  const trValues: number[] = [];

  for (let i = 1; i < candles.length; i++) {
    const current = candles[i];
    const previous = candles[i - 1];

    const tr = Math.max(
      current.high - current.low,
      Math.abs(current.high - previous.close),
      Math.abs(current.low - previous.close)
    );

    trValues.push(tr);
  }

  return calculateEMA(trValues.slice(-period), period);
}
```

### 5.3 信号检测
```typescript
/**
 * 检测交易信号
 * @param candles - K线数据
 * @returns 信号对象或null
 */
function detectSignal(candles: Candle[]): Signal | null {
  if (candles.length < 233) return null;

  const closes = candles.map(c => c.close);
  const ema21 = calculateEMA(closes.slice(-21), 21);
  const ema89 = calculateEMA(closes.slice(-89), 89);
  const ema233 = calculateEMA(closes.slice(-233), 233);

  // 判断趋势
  const trend = ema89 > ema233 ? 'bullish' : 'bearish';

  // 获取前一根K线的EMA
  const prevCloses = candles.slice(0, -1).map(c => c.close);
  const prevEma21 = calculateEMA(prevCloses.slice(-21), 21);
  const prevEma89 = calculateEMA(prevCloses.slice(-89), 89);

  // 检测金叉/死叉
  if (trend === 'bullish' && prevEma21 <= prevEma89 && ema21 > ema89) {
    return {
      direction: 'long',
      entry: candles[candles.length - 1].close,
      trend: 'bullish'
    };
  }

  if (trend === 'bearish' && prevEma21 >= prevEma89 && ema21 < ema89) {
    return {
      direction: 'short',
      entry: candles[candles.length - 1].close,
      trend: 'bearish'
    };
  }

  return null;
}
```

---

## 六、MT4/MT5对接方案

### 6.1 MetaAPI配置
```typescript
// src/config/mt4.ts
export default {
  token: process.env.METAAPI_TOKEN,
  accountId: process.env.METAAPI_ACCOUNT_ID,
  // 测试账号信息
  testAccount: {
    account: '27238218',
    server: 'VTMarkets-Live 8',
    password: 'Abc1234@'
  }
};
```

### 6.2 连接示例
```typescript
import { MetaApi } from '@metaapi.cloud/sdk';

const api = new MetaApi(token);
const account = await api.metatraderAccountApi.getAccount(accountId);

const connection = await account.getStreamingConnection();
await connection.connect();
await connection.waitSynchronized();

// 获取持仓
const positions = await connection.getPositions();
```

---

## 七、部署架构

### 7.1 服务器配置
```
云服务器：阿里云/腾讯云
CPU: 2核
内存: 4GB
带宽: 5Mbps
系统: Ubuntu 22.04 LTS
存储: 40GB SSD
```

### 7.2 部署流程
```
1. 克隆代码
   git clone https://github.com/xxx/goldpilot.git

2. 安装依赖
   cd goldpilot-frontend && npm install
   cd ../goldpilot-backend && npm install

3. 构建前端
   npm run build

4. 启动后端
   pm2 start ecosystem.config.js

5. 配置Nginx反向代理
```

### 7.3 环境变量
```bash
# 后端 .env
NODE_ENV=production
PORT=3000
MONGODB_URI=mongodb://localhost:27017/goldpilot
METAAPI_TOKEN=your_token
METAAPI_ACCOUNT=your_account
ALPHAVANTAGE_KEY=your_key

# 前端 .env
VITE_API_URL=http://your-domain.com
VITE_WS_URL=ws://your-domain.com
```

---

## 八、性能优化

### 8.1 前端优化
- 代码分割
- 懒加载
- 防抖节流
- WebSocket重连机制

### 8.2 后端优化
- 数据库索引
- API响应缓存
- 连接池管理
- PM2集群模式

---

## 九、安全措施

### 9.1 前端安全
- XSS防护
- CSRF防护
- 环境变量隔离

### 9.2 后端安全
- Helmet安全头
- 限流保护
- 输入验证
- 错误处理
